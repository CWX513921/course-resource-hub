# 课程资源共享平台产品需求文档

## 项目概述
- **项目背景与目标**：解决教育资源分散、共享不便的问题，构建一个统一的课程资源共享平台，促进知识流动与高效学习。
- **目标用户群体**：高校/培训机构教师（资源提供者）、在校学生（资源使用者）。
- **核心价值定位**：为教师提供便捷的课件发布与管理工具，为学生提供系统化的学习资源获取与管理渠道，通过数据统计辅助教学改进。

## 功能需求（核心章节）
- **功能模块划分**：
    - 用户认证与权限模块
    - 课程资源管理模块
    - 资源分类与标签模块
    - 用户收藏与历史模块
    - 访问统计与报表模块
- **详细功能列表**：
    - **用户登录/注册**：支持邮箱/手机号注册，账号密码登录。
    - **资源上传**：教师可上传课件（PDF、PPT、Word等），填写标题、描述、所属课程、标签。
    - **资源浏览与查询**：所有用户可按分类、标签、关键词、上传者进行条件筛选和搜索。
    - **资源下载与预览**：学生可在线预览或下载资源，下载记录被统计。
    - **资源收藏**：学生可收藏资源至个人中心，支持取消收藏。
    - **资源分类管理**：管理员可增删改查一级分类和二级标签。
    - **访问统计**：统计资源被浏览、下载、收藏的次数，生成图表。
    - **用户管理**：管理员可查看用户列表，禁用/启用账号。
- **功能优先级排序**：
    - P0（核心）：用户登录/注册、资源上传/下载/浏览、基础分类与查询。
    - P1（重要）：资源收藏、访问统计（基础计数）、用户管理。
    - P2（优化）：高级统计报表、资源评论、标签推荐。
- **功能依赖关系**：
    - 资源上传、下载、收藏依赖于用户登录状态。
    - 访问统计依赖于资源浏览、下载、收藏行为的记录。
    - 分类管理功能仅对管理员角色开放。

## 技术建议
- **技术栈选型理由**：
    - **前端 (Vue 3 + Element Plus)**：生态成熟，开发效率高，组件库丰富，适合构建管理类Web应用。
    - **后端 (Node.js + Express)**：与前端技术栈统一语言，轻量高效，适合RESTful API开发。
    - **数据库 (MySQL)**：关系型数据库，满足结构化数据存储需求，事务支持好。
    - **部署 (Nginx)**：高性能反向代理和Web服务器，用于前端部署和API请求转发。
- **架构设计建议**：
    - 采用前后端分离架构，通过RESTful API进行数据交互。
    - 后端采用分层架构：路由层、控制器层、服务层、数据访问层。
    - 数据库设计遵循第三范式，建立合理的外键关联和索引。
- **关键技术决策**：
    - 用户认证采用JWT（JSON Web Token）实现无状态登录。
    - 文件上传存储采用本地文件系统或对象存储（如MinIO），数据库记录文件元信息。
    - 统计数据采用定时任务或触发式更新，避免实时计算影响性能。

## 验收标准
- **功能验收标准**：
    - 所有P0、P1功能完整可用，操作流程闭环无卡顿。
    - 用户角色权限控制准确，教师无法访问管理后台。
    - 资源CRUD操作成功，数据持久化正确。
    - 条件查询与搜索结果准确。
- **性能验收标准**：
    - 页面首次加载时间小于3秒。
    - API接口平均响应时间小于500毫秒。
    - 支持至少50个并发用户访问。
- **安全验收标准**：
    - 用户密码经哈希加密存储。
    - API接口有基本的权限校验和防SQL注入、XSS攻击措施。
    - 敏感操作（如删除）有二次确认。

## 功能流程
- **核心业务流程图**：
    - 教师：登录 -> 进入工作台 -> 上传资源 -> 填写信息 -> 提交 -> 资源进入列表。
    - 学生：登录 -> 浏览/搜索资源 -> 预览/下载/收藏 -> 操作记录被统计。
- **用户操作流程**：
    - 新用户注册流程：访问注册页 -> 填写信息 -> 邮箱/手机验证 -> 注册成功并自动登录。
    - 资源搜索流程：在搜索框输入关键词 -> 选择筛选条件（分类、标签） -> 点击搜索 -> 展示结果列表。
- **异常处理流程**：
    - 登录失败：提示账号或密码错误，连续错误5次锁定账号15分钟。
    - 文件上传失败：提示文件格式或大小限制，返回具体错误原因。
    - 资源不存在：访问已删除资源链接时，跳转至404提示页面。

## 用户交互设计
- **交互设计原则**：
    - 一致性：遵循Element Plus设计规范，保持界面风格、操作逻辑统一。
    - 反馈性：所有用户操作（如点击、提交）均需给予明确的状态反馈（加载、成功、失败）。
    - 易用性：核心功能入口明确，操作步骤简洁，提供必要的操作引导。
- **关键界面设计要点**：
    - **首页**：突出搜索功能，清晰展示资源分类导航和最新/热门资源。
    - **资源详情页**：信息布局清晰，突出下载/收藏按钮，展示统计信息。
    - **教师工作台**：采用卡片式布局管理上传的资源，状态（已发布/草稿）区分明显。
- **用户体验优化建议**：
    - 为资源列表提供多种视图切换（列表视图/卡片视图）。
    - 搜索框支持搜索历史记录和热门搜索推荐。
    - 上传文件时提供进度条显示。

## 界面原型建议
- **核心页面布局建议**：
    - **通用布局**：顶部导航栏（Logo、搜索、用户菜单）、左侧分类导航（可折叠）、右侧主内容区、底部页脚。
    - **登录/注册页**：居中卡片式表单，简洁无干扰。
    - **资源管理页（教师）**：顶部操作栏（上传按钮、筛选条件），下方为资源表格或卡片列表。
- **组件设计规范**：
    - 按钮：主要操作（如提交、下载）使用主按钮，次要操作使用次按钮，危险操作（如删除）使用红色提示。
    - 表单：标签对齐，必填项标星，即时校验并提示错误。
    - 表格：支持排序、分页，操作列固定。