# 课程资源共享平台开发任务清单

## T-01: 项目初始化与基础架构搭建

**类型**: 部署/后端/前端
**优先级**: P0
**依赖**: 无
**预估工作量**: 中
**描述**: 初始化前后端项目，配置开发环境，搭建基础技术框架。包括Vue 3 + Element Plus前端项目、Node.js + Express后端项目、MySQL数据库连接配置，以及Nginx反向代理基础配置。
**验收标准**:
- [ ] 前端项目成功运行，能访问默认欢迎页。
- [ ] 后端服务成功启动，健康检查接口（如`/api/health`）返回正常。
- [ ] 后端能成功连接MySQL数据库。
- [ ] Nginx配置完成，能将`/api`请求代理到后端服务。

## T-02: 数据库设计与迁移脚本

**类型**: 数据库
**优先级**: P0
**依赖**: T-01
**预估工作量**: 中
**描述**: 根据API文档和PRD设计数据库表结构（users, resources, categories, tags, favorites, download_logs等），并编写初始化SQL脚本和迁移工具。
**验收标准**:
- [ ] 所有核心表（用户、资源、分类、标签、收藏、下载记录）创建成功。
- [ ] 表间外键关系、索引（如用户名、资源标题）设置正确。
- [ ] 能执行初始化脚本，插入必要的默认数据（如管理员账号、基础分类）。

## T-03: 用户认证与授权模块实现

**类型**: 后端
**优先级**: P0
**依赖**: T-02
**预估工作量**: 大
**描述**: 实现用户注册、登录、JWT Token生成与验证中间件。基于角色的权限控制（RBAC）中间件，区分学生、教师、管理员权限。
**验收标准**:
- [ ] 提供`/api/v1/auth/register`和`/api/v1/auth/login`接口，功能符合API契约。
- [ ] 登录成功返回有效JWT Token，Token中包含用户ID和角色信息。
- [ ] 实现权限中间件，能保护需要登录或特定角色的路由（如教师工作台、管理后台）。
- [ ] 密码使用哈希加密存储。

## T-04: 课程资源管理后端API

**类型**: 后端
**优先级**: P0
**依赖**: T-02, T-03
**预估工作量**: 大
**描述**: 实现资源的完整CRUD API。包括资源创建（处理文件上传）、资源列表查询（支持分类、标签、关键词筛选和分页）、资源详情获取、资源更新与删除。
**验收标准**:
- [ ] `POST /api/v1/resources`接口能接收文件上传，并将文件信息存入数据库。
- [ ] `GET /api/v1/resources`接口支持按`category`、`tag`、`keyword`参数筛选，并返回分页结果。
- [ ] `GET /api/v1/resources/:id`返回资源详情，包含上传者信息。
- [ ] `PUT`和`DELETE`接口实现权限控制（仅资源上传者或管理员可操作）。

## T-05: 资源浏览与查询前端页面

**类型**: 前端
**优先级**: P0
**依赖**: T-04
**预估工作量**: 大
**描述**: 开发首页资源列表页和资源详情页。实现资源卡片/列表展示、分类导航、搜索栏、分页功能。资源详情页展示完整信息并提供下载/收藏入口。
**验收标准**:
- [ ] 首页能展示资源列表，支持按分类树进行筛选。
- [ ] 搜索栏输入关键词后，能触发搜索并更新列表。
- [ ] 点击资源卡片能跳转至详情页，展示所有字段信息。
- [ ] 列表页和详情页的UI符合Element Plus设计规范。

## T-06: 资源上传与管理前端页面

**类型**: 前端
**优先级**: P0
**依赖**: T-04, T-05
**预估工作量**: 大
**描述**: 开发教师工作台页面，实现资源上传表单（含文件选择器、分类标签选择）、已上传资源列表管理（查看、编辑、删除）。
**验收标准**:
- [ ] 上传表单能选择文件，并填写标题、描述、分类、标签等信息。
- [ ] 表单提交成功后，资源出现在工作台列表中。
- [ ] 工作台列表支持按状态（如已发布）筛选，并提供编辑和删除操作按钮。
- [ ] 删除操作需有二次确认弹窗。

## T-07: 资源收藏功能实现

**类型**: 后端/前端
**优先级**: P1
**依赖**: T-03, T-05
**预估工作量**: 中
**描述**: 后端实现收藏/取消收藏API，前端在资源详情页和用户中心添加收藏交互逻辑与列表展示。
**验收标准**:
- [ ] 后端`POST /api/v1/favorites/:resourceId`和`DELETE`接口工作正常，记录用户收藏关系。
- [ ] 资源详情页根据当前用户收藏状态，动态显示“收藏”或“取消收藏”按钮。
- [ ] 用户中心页面能列出当前用户收藏的所有资源。

## T-08: 访问统计与基础报表

**类型**: 后端/前端
**优先级**: P1
**依赖**: T-04, T-05
**预估工作量**: 大
**描述**: 后端实现资源浏览、下载行为记录接口，并提供统计查询API。前端在管理后台或教师工作台展示统计图表（如下载量Top10、分类资源数量）。
**验收标准**:
- [ ] 资源详情页被访问时，后端能记录浏览次数（可选）。
- [ ] 调用下载接口时，后端能更新资源的`download_count`。
- [ ] 提供`/api/v1/stats/overview`和`/api/v1/stats/resources/top-downloaded`等统计接口。
- [ ] 前端使用ECharts等图表库，成功渲染至少2种统计图表（如柱状图、饼图）。

## T-09: 核心功能集成与测试

**类型**: 测试
**优先级**: P2
**依赖**: T-06, T-07, T-08
**预估工作量**: 中
**描述**: 对核心业务流程进行集成测试，包括用户登录、资源上传、搜索、下载、收藏的完整闭环。编写关键模块的单元测试。
**验收标准**:
- [ ] 完成“教师上传-学生搜索-下载-收藏”全流程手动测试，无阻塞性BUG。
- [ ] 核心后端服务函数（如密码验证、权限校验）有单元测试覆盖。
- [ ] API接口通过Postman或类似工具测试，符合契约文档。

## T-10: 系统部署与文档完善

**类型**: 部署
**优先级**: P2
**依赖**: T-09
**预估工作量**: 小
**描述**: 编写部署脚本，配置生产环境Nginx，整理项目文档（README、API文档、部署手册）。
**验收标准**:
- [ ] 提供一键启动脚本（如`docker-compose up`或`npm run deploy`）。
- [ ] 生产环境Nginx配置完善，支持前端History路由和API代理。
- [ ] 项目README包含本地开发、构建、部署的清晰说明。
- [ ] 最终输出的API文档与实际接口一致。

---
**任务依赖关系图**:
T-01 -> T-02 -> T-03 -> T-04 -> T-05 -> T-06
                          |-> T-07
                          |-> T-08
T-06, T-07, T-08 -> T-09 -> T-10